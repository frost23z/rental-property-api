pre-commit:
  piped: true 
  commands:
    format:
      glob: "**/*.go"
      run: go tool goimports -w {staged_files} && git add {staged_files}

pre-push:
  piped: true
  commands:
    go-vet:
      run: go vet ./...
    go-test:
      run: go tool bee generate routers && go test -v ./...
